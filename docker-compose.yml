services:
  api:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: freelancer-marketplace-api

    ports:
      - "8000:8000"

    env_file:
      - .env

    environment:
      PORT: 8000

    restart: unless-stopped